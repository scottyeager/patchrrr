[project]
name = "patchrrr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Scott Yeager", email = "yeagersm@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["patchrrr.py"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
